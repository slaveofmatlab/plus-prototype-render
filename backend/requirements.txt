# ============================================
# 报价建议 Demo - 后端环境依赖
# 安装: pip install -r requirements.txt
# ============================================

# --- Web 框架 ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# --- 数据处理 ---
pandas>=1.3.0
openpyxl>=3.0.0

# --- 匹配算法依赖 ---
jieba>=0.42.1
opencc-python-reimplemented>=0.1.7  # 繁体转简体（特殊客户预处理）

# --- 可选加速（未安装时自动回退，不影响功能） ---
python-calamine>=0.2.0      # 加速 Excel 读取（回退 openpyxl）
pyahocorasick>=2.0.0        # 加速品牌检测（回退线性扫描）
xlsxwriter>=3.0.0           # 加速 Excel 写入（回退 openpyxl）
